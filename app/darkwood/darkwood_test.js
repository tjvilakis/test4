'use strict';

describe('myApp.darkwood module', function() {

  beforeEach(module('myApp.darkwood'));

  describe('darkwood controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('darkwoodCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});