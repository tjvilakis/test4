'use strict';

describe('downloads module', function() {

  beforeEach(module('myApp.view1'));

  describe('view3 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var downloadsCtrl = $controller('downloadsCtrl');
      expect(downloadsCtrl).toBeDefined();
    }));

  });
});